// Thin re-export: the actual implementation lives in the platform component
// library. Kept here so feature files can import from within the feature boundary.
export { PlatformIcon } from '@/components/platform';
